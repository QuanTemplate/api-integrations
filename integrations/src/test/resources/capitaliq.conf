capitaliq {
  endpoint = "https://test.endpoint"

  # number of mnemonics / identifiers used in a single HTTP request
  mnemonicsPerRequestInDemoAccount = 100
  mnemonicsPerRequestInProdAccount = 500

  demoAccount = true

  credentials {
    username = "capiquser@quantemplate.com"
    password = abcd
  }
}
