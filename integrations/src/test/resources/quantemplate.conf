quantemplate {
  auth {
    endpoint = "https://test.endpoint"

    credentials {
      clientId = u-test-user
      clientSecret = abcd
    }
  }

  api {
    baseUrl = "https://test.endpoint"
  }
}
